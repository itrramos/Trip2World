{
  "name": "@trip2world/ui",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "//": "Ships TypeScript source rather than a build. Both consumers are Next.js apps that already list this package in `transpilePackages`, so a build step would add latency and a stale-dist failure mode for no benefit.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src --ext .ts,.tsx",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@trip2world/types": "workspace:*",
    "clsx": "^2.1.1",
    "lucide-react": "^0.469.0",
    "tailwind-merge": "^2.6.0"
  },
  "peerDependencies": {
    "next": "^15.1.4",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@trip2world/config": "workspace:*",
    "@types/react": "^19.0.7",
    "eslint": "^8.57.1",
    "typescript": "^5.7.3"
  }
}
